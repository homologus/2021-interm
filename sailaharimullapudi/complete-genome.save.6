from Bio.Seq import Seq
from Bio import SeqIO 
from Bio.SeqUtils import GC
 
my_seq = Seq("")

for i in SeqIO.parse("/share/SARS-class/SARS-2020.fasta", "fasta"):
	#print(i)
	#print(len(i.seq))
	#print(GC(i.seq))

	my_seq = (i.seq.translate().split("*"))
	print(my_seq)

	#print(i[1:].seq.translate().split("*"))
	#print(i[2:].seq.translate().split("*"))
	
		
	
